string = input("Enter start number: ")
start = string + 0
current = start
sequence = string

while current != 1
	sequence ..= " -> "
	if current % 2 == 0
		current /= 2
	else
		current = (current * 3) + 1

	sequence ..= current

print(sequence)
