number = input("Enter a number: ")
total = 0
equation = ""

i = 1
while i < number + 1
	total = total + i
	equation ..= i
	if i .. "" != number
		equation ..= " + "
	i += 1

print(equation .. " = " .. total)
