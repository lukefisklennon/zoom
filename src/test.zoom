function wonder(number)
	if number % 2 == 0
		number /= 2
	else
		number = (number * 3) + 1
	return number

string = input("Enter start number: ")
start = string + 0
current = start
sequence = string

while current != 1
	current = wonder(current)
	sequence ..= " -> " .. current

print(sequence)
